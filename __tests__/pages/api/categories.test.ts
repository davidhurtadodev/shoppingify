describe('POST/categories', () => {
  describe('when passed a category', () => {
    test('should respond with a 200 status code');
  });
});

describe('without valid category', () => {
  // should respond if cateroy already exist
  // should respond of empty category
});

test('post a category', () => {
  // should save a category to db
  // respond with 200 status
});

test('get categories', () => {
  // should respond with 200 status
  // should get all categories
});
